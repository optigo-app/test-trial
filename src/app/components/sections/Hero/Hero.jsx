
import React from "react";
import VideoSection from "./VideoSection";

const Hero = ({ theme, pageType }) => {

    return (
        <VideoSection theme={theme} pageType={pageType} />
    );
};

export default Hero;
